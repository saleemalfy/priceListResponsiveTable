/*function changePrice() {
    var oldPrice = document.body.innerHTML;
    var something2 = document.getElementsByName("price").innerHTML;
    document.body.innerHTML = somthing1;
    document.body.innerHTML = something2;
}
/*-----------------------------------------*/
/*--if it doesn't work try ByTagName--*/
/*function updatePrice() {
    var oldPrice = document.getElementsByTagNameName("price").innerHTML;
}

$("price").change(function) {
    var value = this.value;
    $("#price")
}*/


/*$('.price').change(function () {
    var price = parseFloat($('.price').data('base-price'));

    $('.price').each(function (i, el) {
        price += parseFloat($('option:selected', el).data('price'));
    });

    $('.price span').text(price.toFixed(2));
});

*/